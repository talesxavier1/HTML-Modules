
const main = (): void => {
    debugger;
    var chartInstance: DevExpress.ui.dxButton;
    chartInstance = $("#teste").dxButton({
        text: "Frutas laminadas",
        type: 'normal',
        height: "100px",
        onClick: () => {
            let a = "";

        }
    }).dxButton("instance")
}

main();
